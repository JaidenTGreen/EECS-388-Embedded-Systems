#include <stdio.h>

int main()
{
    int size;
    int list[] = {};
    int output;
    int check[] = {};

    printf("How big is the arry?: \n");
    scanf("%d", &size);

    for (int i = 0; i < size - 1; i++){
        int next = i++;
        int temp;
        printf("Enter element %d: \n", next);
        scanf("%d", &temp);
        list[i] = temp;
        for (i; i <= 0; i--){
            for (0, )
        }

    }
}